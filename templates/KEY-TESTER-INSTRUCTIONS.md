# Key Tester Instructions

Instructions for users reporting keyboard input bugs:

```bash
git clone https://github.com/badlogic/pi-mono.git
cd pi-mono
npm install
cd packages/tui
npx tsx test/key-tester.ts
```

Then:
1. Press the problematic key combination a few times
2. Copy/paste all the output and share it
