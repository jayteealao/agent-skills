# list-cli

A tiny CLI. `node src/cli.mjs list <dir>` prints the entries of a directory,
one per line. `node src/cli.mjs count <dir>` prints how many entries it holds.

Run the tests with `npm test`.
