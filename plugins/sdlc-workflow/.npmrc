; The commit subject `npm version <level>` writes (docs/internal/RELEASE-DISCIPLINE.md).
; The release guard (scripts/verify-release-pushed.mjs RELEASE_SUBJECT) watches
; for this prefix, so a bump that sits unpushed is reported.
message=release(sdlc-workflow): v%s
