interface:
  display_name: "Review Style Consistency"
  short_description: "Enforce consistency with existing codebase style and language idioms to reduce cognitive load"
  default_prompt: "Use $review-style-consistency for this workflow."
policy:
  allow_implicit_invocation: false
