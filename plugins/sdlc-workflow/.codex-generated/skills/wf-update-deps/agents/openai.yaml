interface:
  display_name: "WF Update Deps"
  short_description: "Dependency update workflow. Scans package manifests for outdated and vulnerable dependencies, researches each via web search, plans updates grouped by risk, implements, and verifies."
  default_prompt: "Use $wf-update-deps for this workflow."
policy:
  allow_implicit_invocation: false
