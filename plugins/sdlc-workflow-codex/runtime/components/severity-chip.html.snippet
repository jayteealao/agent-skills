<span class="sev severity-{{sev}}" aria-label="{{sev}}"><span class="sev-glyph" aria-hidden="true">{{{glyph}}}</span>{{label}}{{#each count}} <span class="sev-count">({{this}})</span>{{/each}}</span>
