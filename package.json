{
  "name": "agent-skills-marketplace",
  "version": "1.48.0",
  "private": true,
  "description": "Local dev workspace for the agent-skills marketplace. Used for ad-hoc test/verification scripts (router migration verifier, replay harness, etc.). Not published.",
  "type": "module",
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.126"
  }
}
